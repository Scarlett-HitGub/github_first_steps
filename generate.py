# Unidad Libraries
# module = (preset) already solved problem that we can use to spend less time coding

# import = allows us to brings a contents from some _module_ of python, in this case _random_

import random
#from random import choice #< just another way to call an especific function from random module


# _random.choice_ an example of those module that python content in it's libraries of module
coin = random.choice(["heads", "tails"])
print(coin)
